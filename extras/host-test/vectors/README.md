# Golden vectors

Empty on purpose. Drop real ESP-NOW captures here as `.bin` (one payload per
file) and add a test that replays them against the decoder, so the codec is
covered end to end and not just field by field. See ../README.md.
